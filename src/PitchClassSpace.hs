module PitchClassSpace where

import Euterpea

{-
This is where my exploration of pitch class space goes.
In particular - *do not care* about pitches (i.e. tuples
of (pc: PitchClass, oct: Octave)) - instead, trying to
examine musical pitches "modulo" the octave they lie in.
-}

